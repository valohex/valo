setTimeout(() => {
    if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
    setTimeout(() => {
        if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
        setTimeout(() => {
            if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
            setTimeout(() => {
                if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
                setTimeout(() => {
                    if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
                    setTimeout(() => {
                        if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
                        setTimeout(() => {
                            if(document.querySelector('.hide_header')) document.querySelector('.hide_header').classList.remove("hide_header");
                        }, 200);
                    }, 200);
                }, 200);
            }, 200);
        }, 200);
    }, 200);
}, 200);